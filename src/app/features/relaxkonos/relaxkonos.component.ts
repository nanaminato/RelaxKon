import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';
@Component({ imports: [RouterLink], template: `<section class="page"><div class="container"><p class="eyebrow">Product / RelaxKonOS</p><h1>Desktops beyond devices.</h1><p class="lead">RelaxKonOS is a cross-platform, cloud-native desktop operating environment. It uses state synchronization instead of RDP, VNC, or pixel streaming.</p><div class="card-grid sections"><article class="card"><h3>Client</h3><p>Desktop shell, window management, application UI, input handling, and local rendering.</p></article><article class="card"><h3>Server</h3><p>Identity, workspace, storage, synchronization, remote runtime, and compute services.</p></article><article class="card"><h3>Protocol</h3><p>Explicit REST and SignalR contracts connecting client experiences to remote capabilities.</p></article></div><p class="notice">The server does not render or transmit a desktop image. It manages the state and services that make the workspace durable.</p><a class="button primary" routerLink="/docs/en-US/latest/concepts/architecture">Read the architecture guide</a></div></section>`, styles: `.sections{margin:2rem 0}.notice{margin:2rem 0}` })
export class RelaxKonOsComponent {}
